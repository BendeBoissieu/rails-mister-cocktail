<div class="container">

    <div class="col-xs-12">
      <h1><%= @cocktail.name %></h1>



     <!--  link_to "Modify the cocktail", root_path  -->

  <div class="row">
    <div class="col-sm-12 col-md-6">
      <h3>Recipe   <span class='glyphicon glyphicon-glass'></span> </h3>
      <table class="table table-bordered">
        <tbody>
          <thead>
            <th>Ingredient </th>
            <th>Description</th>

          </thead>
          <% @cocktail.doses.each do |dose| %>
            <tr>
              <td><%= dose.ingredient.name %></td>
              <td><%= dose.description %></td>
            </tr>
          <% end %>
    </div>
        </tbody>
      </table>


    </div>

  </div>
</div>


<%= cl_image_tag(@cocktail.name,
      width: 200, height: 150, crop: :pad) %>

<!-- Does not work:
div class="card"
  style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)),
         url('%= cl_image_path @cocktail.photo, height: 300, width: 400, crop: :fill %')"
-->
<!-- Does not work:
%= image_tag @cocktail.photo.url(:bright_face) %
-->

    <h4><%= link_to " Edit a cocktail ", edit_cocktail_path, class: 'glyphicon glyphicon-hand-right'%></h4>
   <h4><%= link_to " Back to all cocktails ", root_path, class: 'glyphicon glyphicon-hand-right'%></h4>





<div class="button aqua">
  <div class="glare"></div>
    <p><%= link_to " Edit a cocktail ", edit_cocktail_path, class: 'glyphicon glyphicon-hand-right'%></p>
</div>






